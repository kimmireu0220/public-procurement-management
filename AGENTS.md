# 공공조달관리사 — 최소 운영 원칙

이 저장소는 공공조달관리사 시험 학습을 지원한다. 에이전트는 사용자의 목표에 맞춰
자료의 구성, 분량, 형식, 제작 순서, 도구와 검증 범위를 스스로 정한다. 기존 문서와
산출물의 형식은 참고 사례이며, 사용자 요청이나 작업 목적보다 우선하지 않는다.

## 반드시 지킬 것

### 정확성과 근거

- 시험 학습의 기준 자료는 조달청 표준교재다. 시험 일정·형식·합격 기준은
  `docs/시험_안내.md`와 `sources/공식_qnet_시행공고/`에서 확인한다.
- 표준교재와 현행 법령·고시가 다르면 둘을 혼동시키지 않는다. 표준교재의 시험 기준과
  현행 규정의 시행일을 독자가 구별할 수 있게 밝힌다. 표현 형식은 자유다.
- 표준교재 안의 서술이 충돌하면 임의로 한쪽을 확정하지 말고, 현행 공식 원문과 맥락을
  확인한 뒤 충돌 사실과 판단 근거를 알린다.
- 숫자·기한·비율, 정답, 법적 효과처럼 오류 비용이 큰 내용은 관련 원문이나 프로젝트의
  근거 자료와 대조한다. 확인되지 않은 내용을 사실처럼 만들지 않는다.
- 기존 CBT 문항을 사용할 때는 문제·정답·식별자의 대응이 깨지지 않게 한다. 자체 문항을
  만들거나 변형할 수 있으며, 그 경우 근거와 자체 작성임을 구별한다.

### 공개와 권리

- 공식 자료도 공개·재배포 조건을 확인하고 필요한 출처와 기준일을 남긴다.
- 음성 복제는 화자 또는 권리자의 허가가 확인된 참조 음성에만 사용하며, 사칭·기만·인증
  우회에 사용하지 않는다. 참조 음성을 외부 서비스에 보내거나 결과를 공개하려면 그 범위의
  권한도 확인한다.

### 사용자 권한과 변경

- 사용자의 명시적 범위·형식·분량 지시가 프로젝트의 기본값보다 우선한다.
- 답안 제출만으로 커밋·푸시하거나 외부에 게시하지 않는다. 사용자가 명시적으로 요청한
  경우에만 해당 변경을 수행한다.
- 기존 빌더를 사용할 때는 그 입력 스키마를 맞추되, 작업에 도움이 된다면 스키마와 도구
  자체를 개선할 수 있다. 변경 성격에 맞는 최소한의 검증을 실행한다.

## 작업 기본값 — 필요하면 자유롭게 변경

- 이론 범위 탐색: `output/standard_textbook/<과목>/INDEX.md`와 장별 Markdown
- 숫자 확인: `docs/학습_숫자암기/`
- 현행 규정 근거: `sources/현행_법령_근거/manifest.json`
- 기존 필기 문제·정답 확인: `output/problem_book_final/`, `output/agent_extract/`
- 모의고사와 CBT: `output/mock_exam/`, 공개본은 `docs/`
- 강의 원본: `output/chapter_lectures/`; 공개본 생성은 `tools/build_lecture_pages.py`
- 한국어 Qwen3-TTS Sohee 음성 작업: `.agents/skills/qwen3-tts-korean-lecture/`

강의는 고정 목차나 분량을 요구하지 않는다. 사례, 비교표, 이야기, 질문, O/X, 회상 연습,
문제풀이, 요약 등 학습에 유용한 방식을 자유롭게 사용할 수 있다. 모의고사도 목적에 따라
문항 수, 유형, 출처 배합, 재출제, 자동화 방식을 정한다. 다만 실제 시험을 재현한다고 표시한
경우에는 `docs/시험_안내.md`의 공식 형식을 따른다.

검증 명령은 작업과 관련된 것만 선택한다.

```bash
python3 tools/build_study_cbt.py --check
python3 tools/build_lecture_pages.py --check
python3 tools/private_source_inventory.py
python3 tools/verify_legal_sources.py
python3 tools/validate_mock_exam.py
python3 -m unittest discover -s tests -v
```
