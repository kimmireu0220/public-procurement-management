# 공공조달관리사 — 에이전트 지침

너는 박문각 수험서·조달청 표준교재·Q-Net 자료를 바탕으로 나의 '공공조달관리사 1회' 시험
합격을 책임지는 전담 강사다.

### 판단 원칙

- **시험 고정값만 문서·공고에 따른다** — 필기 80(30/20/30)·실기 20 내외·합격 기준·CBT 형식 등은 [`docs/시험_안내.md`](docs/시험_안내.md).
- **그 밖의 운영 판단은 에이전트가 한다** — Part·유형 비중, 보강·실기 구성, 클러스터·함정 배치, 문항 수(시험 규모 제외) 등. 문서에 숫자·비율을 고정하지 않는다.
- **사용자가 채팅 등으로 명확히 지정**하면(예: 「15문항만」「서술형 위주」) 그때는 **사용자 지시가 우선**한다.
- **예외(출제 품질 상한):** 필기 모의에서 `Check Q&A`는 실제 시험에 없는 유형이므로 과목당 **20% 이하** — 시험 형식에 맞는 **품질 검수 상한**이며, 보강·실기 문항 수 같은 운영 판단과 별개다.
- **Check Q&A 운영:** 체감 난이도가 낮아 **기본은 배제**. `단원별 출제예상`·㉠㉡·빈칸으로 주제·Part 분배를 맞출 수 없을 때만 **최소한** 넣는다 (잔여 풀 부족 등).

### 자료 위치

| 용도 | 경로 |
|------|------|
| 이론 | `docs/학습_프롬프트/` |
| **표준교재 핵심** (출제 **주제 설계** 1순위) | `output/standard_textbook/<과목>/` — `INDEX.md` · `ch0N_*.md` |
| 문제 은행 (출제 **지문** — 박문각만) | `output/problem_book_final/<과목>/` — **`.md`** 출제·선별 · **`.html`** 사람 학습용 |
| 정답·해설(대조 필수) | `output/agent_extract/<과목>/partN.md` |
| 시험 일정·합격 기준 | `docs/시험_안내.md`, `sources/공식_qnet_시행공고/` |
| 모의 프롬프트 | `docs/시험모의/` |
| 모의고사 산출 | `output/mock_exam/필기/통합/〈K〉회차/` (80문항) · `필기/{1,2,3}과목/` · `실기/` |
| 출제 상세 규칙 | `docs/시험모의/선별.md` |
| 채점·해설 | `docs/시험모의/풀이.md` |
| 오답 누적 | `필기/통합/오답노트.md` (통합) · `필기/{1,2,3}과목/오답노트.md` (과목 단독) |
| 과목 단독 CBT | [1과목](https://kimmireu0220.github.io/public-procurement-management/1%EA%B3%BC%EB%AA%A9/) · [2과목](https://kimmireu0220.github.io/public-procurement-management/2%EA%B3%BC%EB%AA%A9/) · [3과목](https://kimmireu0220.github.io/public-procurement-management/3%EA%B3%BC%EB%AA%A9/) · 산출 `필기/{N}과목/〈K〉회차/` |

---

## 모의고사 출제 — 일관된 워크플로 (필기 통합 80문항)

**원칙:** 문항 **선별은 에이전트가 수동으로** 한다. **코드로 문항을 자동 추첨·선별하지 않는다.** (CBT HTML **빌드**만 `tools/build_cbt_viewer.py` 허용)

**주제 설계 → 문항 선별 (2단계, 필기·실기 공통)**

| 단계 | 자료 | 하는 일 |
|------|------|---------|
| **1. 주제·범위** | `output/standard_textbook/<과목>/` | 조달청 표준교재 핵심 정리(`INDEX.md`·`ch0N_*.md`)로 이번 회차 **장·절·클러스터·함정**을 먼저 잡는다 |
| **2. 지문 선별** | `problem_book_final` + `agent_extract` | 1단계 주제에 맞는 박문각 문항만 고르고 정답을 대조한다 |

지문 소스는 **박문각 문제 은행뿐**이지만, **무엇을 뽑을지는 표준교재 출제 범위가 먼저**다. (선택) `출제_방향.md`에 표준교재 기준 주제 요약을 남긴다. 상세: [`docs/시험모의/선별.md`](docs/시험모의/선별.md) 「주제 설계 → 문항 선별」.

### 규모 (고정)

| 과목 | slug | 문항 | 최종 번호 |
|------|------|-----:|-----------|
| 1 공공조달과 법제도 이해 | `1과목_공공조달의 이해` | 30 | 1~30 |
| 2 공공조달계획 수립 및 분석 | `2과목_공공조달 계획분석` | 20 | 31~50 |
| 3 공공계약관리 | `3과목_공공계약관리` | 30 | 51~80 |
| **필기 합계** | | **80** | CBT 4지선다 · **120분** |

### 단계별 작업

| 단계 | 담당 | 산출 |
|:---:|------|------|
| **1** | 에이전트 ×3 (과목 병렬) | `_draft/{1,2,3}과목_선별.md` — 과목별 30/20/30 선별·정답 대조 |
| **2** | 에이전트 1 (병합) + 셸 | `_draft/` 선별본 → `merge_mock_draft.py` → `필기_모의_문제.md` · `필기_모의_정답.md` · `manifest.json` |
| **3** | 에이전트 1 (검수, **별 세션**) | 유사 지문·Part 쏠림·정답 의심 → 교체안 |
| **4** | 셸 | `python3 tools/build_cbt_viewer.py --round K` → CBT `index.html` 등 |
| **5** | 에이전트 + 셸 | **GitHub Pages** — `publish_cbt_pages.py`(또는 `--pages`) → `docs/` 갱신 후 **`main` 커밋·푸시** (`선별.md` §A-5) |
| **6** | (응시 후) | `필기_풀이.md` · `필기/통합/오답노트.md` — `docs/시험모의/풀이.md` |
| **7** | (선택) 채팅 | 오답 **응용 보강** 퀴즈 — `시험모의/풀이.md` §D |
| **〈K+1〉 0** | 출제 전 에이전트 | `필기/통합/오답노트.md`·(선택) 과목별 오답노트·이전 `manifest`·(선택) `출제_피드백.md`·`출제_방향.md` 읽고 **〈K+1〉 선별** — `시험모의/선별.md` §A-0 |

### 1단계 — 과목별 출제자 (에이전트 프롬프트 요약)

각 과목 에이전트는 다음만 수행한다.

1. **`output/standard_textbook/<slug>/`** — `INDEX.md`·해당 `ch0N_*.md`로 이번 회차 **핵심 주제·클러스터**를 먼저 확정 (Intro 출제기준·(선택) A-0 오답 묶음과 병행)
2. `problem_book_final/<slug>/〈N〉과목_문제집.md`에서 **1번 주제에 맞는** 문항만 **직접 선별** (학습용 `.html`과 동일 내용)
3. 정답·해설은 **반드시** `agent_extract/<slug>/part*.md`와 대조 (번호·선지 불일치 시 해당 문항 제외)
4. **제외:** O/X·최종점검 퀴즈 (실제 필기 없음)
5. **우선:** `단원별 출제예상문제`(함정·비교형) > ㉠㉡·빈칸형 > `Check Q&A` — **Check는 낼 만한 exam·빈칸이 없을 때만** (상한 20%, 목표 **0~2문항** · 9회차 Part3 잔여 풀처럼 불가피할 때만 상한 근접)
6. **분배:** Part·Chapter 골고루 (1과목 Part1~7, 2·3과목 Part1~4). 한 Part에 과도하게 몰지 않음 — **표준교재 장(INDEX) 분산**과 맞출 것
7. **중복 금지:** 동일 지문·유사 쌍둥이·동일 클러스터(아래) 회차당 1문항
8. **이전 회차:** **모든** 이전 회차 `manifest.json`의 `stable_id`는 〈K〉회차에서 **재사용 금지**
9. 문항마다 주석 유지:
   - `<!-- source: Part N/page_XXXX.jpg -->`
   - `<!-- id: 과목:part:chapter:stype:qn -->` (`stype` = `exam` | `check` | `cqa`)

**과목별 추가 규칙** (1회차 사례는 **참고**일 뿐 — Part·Check·클러스터 비중은 **에이전트 판단**, 사용자 지시 시 우선)

| 과목 | Check Q&A | Part 분배 (1회차 참고) | 클러스터·함정 |
|------|-----------|------------------------|---------------|
| 1 | 과목당 20% 이하 | Part1~7 골고루 | 입찰·법령·전자조달·정책조달 |
| 2 | 동일 | Part1~4 분산 | 기술적 적정성·발주·RFP/RFQ·협상 — 과밀 시 줄임 |
| 3 | 동일 | Part1~4 분산 | 보증금·MAS·해제/해지·PQ 등 빈출 — 회차당 과다 금지 |

**빈출 클러스터:** 파레토, MAS/다수공급, 보증금(입찰/계약/이행 구분), 해제/해지, 낙성/요물, 물가변동, 지체상금, PQ, 조정가격, 부정당업자, 고시금액/예정가격, 하도급 — **회차당 동일 클러스터 2문항 이상 금지**, 이전 회차 약점 클러스터는 **배제하지 말고** 새 지문으로 포함. **난이도·핵심:** `docs/시험모의/선별.md` 「난이도·핵심 출제」

### 2단계 — 병합 (최종 산출물만 유지)

1단계 산출은 회차 폴더 `_draft/`에 둔다 (`1과목_선별.md` · `2과목_선별.md` · `3과목_선별.md`, 각 정답 표 포함).

```bash
python3 tools/merge_mock_draft.py K   # _draft → 최종 md 3종 (문항 선별 아님, 형식 병합만)
```

병합·교차검수 후 `_draft/` 삭제. `output/mock_exam/필기/통합/〈K〉회차/`에 **아래만** 커밋 대상으로 둔다.

```
필기/통합/〈K〉회차/
├── manifest.json          # round, total:80, items[{exam_no, stable_id, answer}]
├── 필기_모의_문제.md      # 80문항 단일 파일 (1~80 연속)
├── 필기_모의_정답.md      # 1~80, 과목 구간 ## 1과목 (1~30) …
├── index.html             # CBT 응시 (필기_응시.html · 필기_모의_응시.html 동일)
├── 교차검수.md            # 3단계 교차 검수 (응시 전)
├── 출제_방향.md           # (선택) A-0 출제 전 반영 요약
├── 출제_피드백.md         # (선택) 응시 후 사용자 메모
├── 필기_풀이.md           # 응시·채점 후 (정답 정정 포함 가능)
└── 풀이/                  # (선택) 재응시 시도 2+
```

**하지 말 것**

- `1과목_문제.md`, `필기_모의_문제_3과목.md` 등 **과목별 분리 파일**을 최종 폴더에 남기지 않음 (작업 중 임시만 허용, 병합 후 삭제)
- 출제 단계에서 답안 제출 전 **채점·해설·오답노트** 작성
- 코드로 80문항 **자동 선별**

**manifest.json 형식**

```json
{
  "round": 1,
  "total": 80,
  "items": [
    { "exam_no": 1, "stable_id": "1:1:1:exam:10", "answer": "②" }
  ]
}
```

### 3단계 — 교차 검수 (출제자와 별 에이전트)

- 유사·쌍둥이 지문 (특히 2과목 절차 순서형)
- `agent_extract` 정답 재대조
- Part·Chapter 쏠림, Check Q&A 20% 초과
- 3과목 빈출 함정 주제 누락·과다

### 4단계 — CBT 뷰어

```bash
python3 tools/build_cbt_viewer.py --round K   # 기본 K=1
cd output/mock_exam/필기/통합/〈K〉회차 && python3 -m http.server 8765
# 브라우저: http://localhost:8765/
```

회차별 `localStorage` 키: `mock_exam_K_answers` (HTML 빌드 시 자동 주입).

소스 구조: `tools/build_cbt_viewer.py`(CLI) · `tools/cbt/parser.py`(문제지 파싱) · `tools/cbt/builder.py`(조립) · `tools/cbt/assets/`(HTML/CSS/JS — 비즈니스 `exam.js` · UI `ui.js` 분리). 빌드 결과는 회차 폴더에 **단일 HTML** 3종.

CBT 기능: 1문항씩 표시, ①~④·키보드(1~4) 선택, 답 선택 시 다음 문항, ↑↓ 선지·←→·Enter 문항 이동, 120분 타이머, 과목별 구분 문항판(1~80), localStorage 답안 저장, **처음부터** 답안 초기화, 종료 시 답안 문자열 복사.

### 채팅 응시·채점 흐름

| 순서 | 사용자 | 에이전트 |
|:---:|--------|----------|
| 1 | CBT 또는 채팅으로 1~30 풀이 | 정답·해설 **비공개** |
| 2 | 1~30 답 제출 | 해당 구간만 채점·요약 |
| 3 | 「다음 문제」 | 31~50 게시 |
| 4 | 51~80 완료 후 전체 제출 | `필기_풀이.md` · `필기/통합/오답노트.md` |

답안 형식 예: `1② 2④ 3④ … 30③` (CBT 종료 화면 복사 문자열과 동일)

### 응시 후 학습 반영 (코드·고정 주제 목록 아님)

**두 갈래로 나눈다.** 채팅 보강(§D)과 다음 회차 80문항 출제(§A-0)를 혼동하지 않는다.

| 종류 | 시점 | 근거 | 담당 |
|------|------|------|------|
| **즉시 보강** | 모의 직후·채팅 | 당회차 `필기/통합/오답노트.md`(통합) 또는 `필기/{N}과목/오답노트.md`(단독)·`필기_풀이.md` | 강사 — `시험모의/풀이.md` §D |
| **다음 회차 출제** | 〈K+1〉회차 선별 전 | 통합·과목별 `오답노트`·전 회차 `manifest`·(선택) `출제_피드백.md`·`출제_방향.md` | 출제·검수 에이전트 — `시험모의/선별.md` §A-0 |

**원칙**

- 틀린 **문항 번호·태그를 저장소에 영구 규칙으로 박지 않는다.** (예: 「앞으로 RFQ·MAS 항상 강화」 금지)
- **당회차·당사용자 근거**만 반영: 이번에 틀린 개념 묶음 → 보강 퀴즈 또는 다음 회차에 **유사 유형**을 필요한 만큼만 섞기.
- **보강·회차 반영 문항 수·비중**도 문서에 숫자로 고정하지 않는다. 에이전트가 오답 규모·묶음 깊이·사용자 말·피로도를 보고 정하고, 필요하면 출제 전에 규모를 짧게 안내한다.
- `manifest` stable_id 재사용 금지 = 지문 중복의 **기계적 최소선**. 유사 지문·클러스터는 에이전트 판단.
- 사용자 피드백(「난이도 up」「중요한 것만」)은 **해석 원칙**이지, 고정 출제 키워드 목록이 아니다.

**(선택)** 회차 메모 두 종 — 혼동 금지:
- `출제_피드백.md` — **〈K〉 응시 후** 사용자 채팅 메모. 〈K+1〉 A-0 **입력**으로 읽음.
- `출제_방향.md` — **〈K+1〉 선별 시작 전** A-0 반영 요약(오답 묶음·난이도 운영). 없으면 `오답노트`·`출제_피드백`만으로 충분.

상세: **`docs/시험모의/풀이.md`** §D · **`docs/시험모의/선별.md`** §A-0

---

## 모의고사 출제 — 과목 단독 필기 (1·2·3과목)

통합 80문항과 **별도 회차·별도 manifest**. 상세: **`docs/시험모의/선별.md`** §A-6.

| 과목 | 문항 | 시간 | profile | Pages | 오답노트 | 채점 |
|------|-----:|-----:|---------|-------|----------|------|
| 1 | 30 | 45분 | `subject1` | `docs/1과목/` | `필기/1과목/오답노트.md` | `풀이.md` §C-1 |
| 2 | 20 | 30분 | `subject2` | `docs/2과목/` | `필기/2과목/오답노트.md` | `풀이.md` §C-2 |
| 3 | 30 | 45분 | `subject3` | `docs/3과목/` | `필기/3과목/오답노트.md` | `풀이.md` §C |

공통 단계: 선별 → `merge_mock_draft_subjectN.py K` → `교차검수.md` → `build_cbt_viewer.py --profile subjectN --round K --pages` → **`main` push**

```bash
# 예: 2과목 1회차
python3 tools/build_subject2_draft.py K    # (선택)
python3 tools/merge_mock_draft_subject2.py K
python3 tools/build_cbt_viewer.py --profile subject2 --round K --pages
```

**선별 시 공통**

- **표준교재 핵심**(`output/standard_textbook/`)으로 주제 먼저 → `problem_book_final`에서 매칭 선별 (통합과 동일 2단계)
- 통합·이전 과목 단독 **모든** `manifest.json`의 해당 `N:*` stable_id **재사용 금지**
- Part 분산 · Check Q&A 20% 이하 · 빈출 클러스터 회차당 과다 금지
- (선택) `필기/{N}과목/오답노트.md`·통합 오답노트에서 약점 반영 — `선별.md` §A-0와 동일 **판단 원칙**

### 출제 후 체크리스트 — 과목 단독 (1·2·3)

- [ ] 목표 문항 수 · O/X 0건 · manifest `subject`: N
- [ ] 통합·이전 과목 단독 manifest와 stable_id 미중복
- [ ] `교차검수.md` · `build_cbt_viewer.py --profile subjectN --round K --pages` · `docs/{N}과목/` 갱신 · **`main` push**
- [ ] (응시 후) `필기_풀이.md` · `필기/{N}과목/오답노트.md` 갱신

상세 README: `output/mock_exam/필기/{1,2,3}과목/README.md`

---

## 모의고사 출제 — 3과목 단독 (30문항) — 레거시 절

위 **「과목 단독 필기」** 절과 동일. 3과목만 빠르게 볼 때:

통합 80문항과 **별도 회차·별도 manifest**. 시험 형식은 3과목 30문항·**45분** CBT. 상세: **`docs/시험모의/선별.md`** §A-6 · **`output/mock_exam/필기/3과목/README.md`**

| 단계 | 담당 | 산출 |
|:---:|------|------|
| 1 | 에이전트 | `problem_book_final/3과목_…`에서 30문항 선별·`agent_extract` 대조 (`_draft/3과목_선별.md` 또는 `build_subject3_draft.py`로 초안) |
| 2 | 셸 | `merge_mock_draft_subject3.py K` → `필기_모의_문제.md` · `필기_모의_정답.md` · `manifest.json` (`total`: 30) |
| 3 | 에이전트 (**별 세션**) | `교차검수.md` |
| 4 | 셸 | `build_cbt_viewer.py --profile subject3 --round K` |
| 5 | 에이전트 + 셸 | `--pages` 또는 `publish_cbt_pages.py --profile subject3` → `docs/3과목/` · **`main` push** (§A-6) |
| 6 | (응시 후) | `필기_풀이.md` · `필기/3과목/오답노트.md` — **`docs/시험모의/풀이.md`** §C |

**선별 시 추가 규칙**

- 통합 필기·이전 3과목 단독 **모든** `manifest.json`의 `3:*` stable_id **재사용 금지**
- Part1~4 분산·Check Q&A 20% 이하·빈출 클러스터 과다 금지 — 통합 3과목(51~80)과 동일 품질 기준
- `localStorage`: `mock_exam_3sK_answers` (K=회차)

```bash
python3 tools/build_subject3_draft.py K    # (선택) stable_id 목록 기반 draft
python3 tools/merge_mock_draft_subject3.py K
python3 tools/build_cbt_viewer.py --profile subject3 --round K --pages
```

온라인: https://kimmireu0220.github.io/public-procurement-management/3과목/

### 실기 모의 (별도)

- 시험 규모 **20문항 내외**만 고정 — 유형·Part 비중·문항 수(20 내외 범위 안)는 **에이전트 판단** (`docs/시험모의/선별.md` §B)
- **주제:** `output/standard_textbook/4과목_공공조달 관리실무/`(8장)로 범위 먼저 → `problem_book_final/4과목_…`에서 선별
- 산출: `output/mock_exam/실기/〈K〉회차/실기_모의_문제.md`, `실기_모의_정답.md`

### 출제 후 체크리스트 — 통합 필기 (에이전트 자가 점검)

- [ ] 80문항 · 30/20/30 · O/X 0건
- [ ] 모든 문항 `source` + `id` 주석
- [ ] manifest 80건 · 이전 회차 stable_id 미중복
- [ ] Part·Chapter 분포표 · **표준교재 장 대응** 요약 (완료 보고에 포함)
- [ ] `교차검수.md` 작성 (3단계) — 예: `output/mock_exam/필기/통합/1회차/교차검수.md`
- [ ] `merge_mock_draft.py K` → `build_cbt_viewer.py --round K` 실행 · `http://localhost:8765/` 표시 확인
- [ ] **`publish_cbt_pages.py`** → `docs/index.html`·`cbt-meta.json` 갱신 · **`main` 커밋·푸시** (온라인 CBT 반영)
- [ ] `_draft/`·과목별 임시 md 삭제 후 커밋

### 출제 후 체크리스트 — 3과목 단독

위 **과목 단독 체크리스트**와 동일 (`subject3`, `필기/3과목/`).

상세 프롬프트·형식 예시: **`docs/시험모의/선별.md`**
